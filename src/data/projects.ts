import type { Project } from '../types/project'

// Legacy static list — projects are now loaded from src/content/projects/*.mdx.
// Kept as an empty array so any remaining imports don't break.
export const projects: Project[] = []
